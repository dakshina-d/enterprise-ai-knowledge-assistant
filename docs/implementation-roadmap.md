# Implementation History

Repository-wide GitHub Actions validation is implemented in `.github/workflows/ci.yml`, covering
the deterministic offline corpus/retrieval pipeline, static quality gates, and full test suite.

Status: **Mandatory PoC phases implemented through phase 24; bounded local deployment phase 25 and
local schema-constrained Qwen/relevance hardening phase 28 are also implemented.** Each retained
phase describes an independently reviewable engineering increment. Optional follow-on work must
keep Ruff, MyPy, Pytest, documentation, and secret scanning green.

Progress: phases 1–22 and the phase-24 acceptance hardening are implemented. The duplicate phase
15 labels are retained as historical commit-plan labels. Phase 23 (HITL), reranking, durable
long-term memory, and feedback persistence are optional and not implemented.

| # | Proposed commit message | Scope | Dependencies | Acceptance criteria and tests | Traceability addressed | Primary risks |
|---:|---|---|---|---|---|---|
| 1 | `docs: finalize architecture and implementation roadmap` | Design contracts, diagrams, ADRs, traceability. | Scaffold | Documents agree; Mermaid/manual review; existing gates pass. | All requirements at design level | Inconsistent terminology or false completion claims. |
| 2 | `feat: add domain models and shared contracts` | IDs, roles, evidence, citations, errors, events, graph-state types. | 1 | Strict validation/serialization; unit and schema snapshot tests. | Input validation, attribution, streaming contracts | Premature or unstable schemas. |
| 3 | `feat: add authentication and rbac foundation` | Local identities/session middleware and backend policy matrix. | 2 | Identity cannot be client-forged; role/IDOR/security tests. | Viewer, analyst, administrator, tool authorization | PoC auth mistaken for production. |
| 4 | `feat: add per-user token bucket rate limiting` | Injectable clock/store, cost/refill, HTTP mapping. | 2, 3 | Concurrent per-user isolation and 429 tests. | Token-bucket rate limiting | Race conditions; multi-process in-memory limitation. |
| 5 | `testdata: generate mock organizational documents` | Deterministic safe corpus and access labels. | 2 | Reproducible, no real data/secrets, schema tests. | Synthetic corpus prerequisite | Accidental sensitive or unrealistic content. |
| 6 | `feat: add document ingestion pipeline` | Parse, normalize, chunk, enrich, hash, batch job. | 2, 5 | Idempotent fixtures; malformed/oversized file tests. | Offline ingestion, metadata, attribution | Parser attack surface; unstable chunks. |
| 7 | `feat: add pinecone dense retrieval` | Dense embeddings, namespace/filter adapter, async query. | 3, 6 | Filter-before-model and failure contract integration tests. | Pinecone namespaces, metadata filtering, async retrieval | Credential/cost leakage; authorization gaps. |
| 8 | `feat: add sparse retrieval and hybrid fusion` | Sparse representation and weighted fusion tuning. | 7 | Fusion unit tests and baseline retrieval evaluation. | Hybrid dense/sparse retrieval | Poor calibration and regression. |
| 9 | `feat: add evidence and citation models` | Provenance IDs, dedupe/diversity, claim-evidence contracts. | 2, 8 | Duplicate and citation mapping tests. | Document attribution, hallucinated-citation protection | Citation/source mismatch. |
| 10 | `feat: add langgraph state and baseline graph` | State invariants, lifecycle nodes, terminal states, fake adapters. | 2, 9 | Compile/run with deterministic fakes; state isolation tests. | LangGraph, async APIs | State mutation and hidden coupling. |
| 11 | `feat: add supervisor routing` | Intent/complexity schemas and allowlisted routes. | 10 | Table-driven routing; invalid model route rejected. | Supervisor agent, response agent | Nondeterminism and unsafe route injection. |
| 12 | `feat: add simple retrieval workflow` | Retrieval agent, knowledge-search node, evidence validation, response draft. | 7–11 | Grounded simple query and no-evidence tests. | Retrieval agent, knowledge search tool | Ungrounded answers; latency. |
| 13 | `feat: add session conversational memory` (implemented) | Owned process-local session store, bounded history/load/update and deterministic follow-ups. | 3, 10 | Multi-turn, bounds, idempotency, concurrency, sanitization, and cross-user isolation tests. | Session memory, multi-turn conversation | Non-durable and non-distributed by design. |
| 14 | `feat: add restricted Python analysis tool` (implemented) | Typed allowlisted incident aggregates, RBAC, taxonomy, provenance, and graph route. | 3, 10, 13 | Authorization, extraction, operation, limits, determinism, and prohibited-code tests. | Python analysis | Arbitrary Python intentionally prohibited. |
| 15 | `feat: add grounded LLM response generation and citation validation` (implemented) | Provider protocol, Responses API, structured claims, repair and fallback. | 10, 13, 14 | Offline provider, prompt, citation, repair, graph and injection tests. | Grounded answers | Live provider remains opt-in; semantic entailment validation remains limited. |
| 15 | `feat: add constrained mcp server and client` (implemented) | Three typed read-only fictional service tools, official-SDK protocol, RBAC, graph route, safe events/traces. | 3, 10 | Contract, allowlist, authorization, security, timeout, cancellation, graph, and malformed-result tests. | MCP tool, MCP failure handling | Local host is the security boundary; no remote OAuth. |
| 16 | `feat: add bounded recursive research workflow` | Implemented in graph 1.2: catalog, compiler, Send workers, recursive rounds, analysis, ledger, coverage and offline evaluation. | 12, 14, 15 | Depth/concurrency/partial-worker routing tests. | Research agent, RLM decomposition, recursive sub-analysis | Server budgets and RBAC bound cost and scope. |
| 17 | `feat: validate citations before response completion` | Claim mapping, support check, one repair path. | 9, 12, 16 | Fabricated/unused/unsupported citation tests. | Citation protection, attribution | False negatives/positives. |
| 18 | `feat: add prompt and retrieved-content guardrails` | Input/evidence injection classifiers and deterministic containment. | 12, 16 | Adversarial prompt/document suite; policy bypass tests. | Prompt injection, retrieved-content validation | Overblocking and classifier reliance. |
| 19 | `feat: add brand and response validation` | Configured response policy and safe refusal/repair. | 17, 18 | Policy corpus and streamed-final consistency tests. | Brand-safety validation | Subjective policy and streaming leakage. |
| 20 | `feat: add langsmith observability` | Trace adapter, spans, correlation, redaction. | 10–19 | Offline schema/redaction tests and real LangSmith smoke traces pass. | Implemented and remotely verified | Sensitive telemetry and vendor outage isolated by allowlisting and failure containment. |
| 21 | `feat: add async chat API and SSE streaming` (implemented) | Authenticated JSON chat, native POST SSE, safe envelope, sequence/terminal semantics, cancellation, lifespan. | 2, 10, 20 | Ordering, disconnect, redaction, lifecycle and API tests. | Real-time workflow and Agent Activity streaming through SSE, async APIs | Durable replay, proxy validation, and multi-worker state remain future work. |
| 22 | `feat: add Streamlit chat UI and agent activity panel` (implemented) | Login, per-session multi-turn display, POST SSE validation, activity, citations/provenance, safe failures and logout. | 3, 13, 21 | Offline client/state/parser/UI tests plus API regressions. | Streamlit chat and real-time Agent Activity | No durable replay, token refresh, cross-device history, or external IdP. |
| 23 | `feat: add human approval graph checkpoint` | Configurable pause/resume/expiry for risky tools. | 16, 21, 22 | Approve/deny/timeout/replay tests. | Human-in-the-loop bonus | Stale approvals and confused deputy. |
| 24 | `test: complete assessment hardening and failure coverage` (implemented) | Deterministic guardrails, structured outcome logging, dependency fault adapters, role/API acceptance and compliance audit. | 12–22 | LLM/retrieval/MCP/Python/API/tracing failures meet safe contracts; security and role matrices pass. | All mandatory failure/security requirements | Overclaiming beyond the bounded offline PoC. |
| 25 | `build: finalize local deployment` (implemented for bounded PoC) | One secure image, API/UI Compose services, Python health smoke, local environment generator, and final architecture. MCP/analysis remain honest local application boundaries rather than invented services. | 14, 15, 22, 24 | Compose config and local image build/start/API health/UI health/down lifecycle pass. | Docker Compose packaging and local operations | False production equivalence; secret handling. |
| 26 | `test: add retrieval and response quality evaluation` | Versioned dataset, metrics, regression thresholds, optional reranking experiment. | 5–19 | Reproducible report and authorization precision 100%. | Reranking bonus, retrieval/citation quality | Overfitting and weak ground truth. |
| 28 | `feat: add local Qwen3 Instruct inference and enforce retrieval relevance` (implemented) | Native Ollama provider, schema-constrained Qwen output, private-reasoning rejection, sparse relevance abstention, verified local query, and local CPU profile. | 8, 15, 24, 25 | Mocked/privacy/live Ollama tests; unsupported password query abstains; supported runbook query cites current evidence; all provider regressions pass. | Local model, grounding, privacy, retrieval quality | CPU latency and lexical-gate calibration |

Long-term memory and the feedback loop remain optional follow-on commits after session-memory/privacy and feedback-contract validation. If implemented, use separate commits: `feat: add consented long-term memory` and `feat: add feedback analysis loop`, each with deletion, isolation, audit, and evaluation tests. They must not be silently folded into unrelated phases.
